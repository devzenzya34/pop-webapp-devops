apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  labels:
    env: prod
    app: postgres
    type: local
spec:
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteOnce